package WS1.Observables;

public class PressureTrendSensor {
    private  int lastReading1;
    private int lastReading2;
    private int lastReading3;

    /*
     * This function returns 1 if trend is raising,
     * 0 if it's stable,
     * and -1 if it's falling.
     * */
    public int calc(){

        return 0;
    }
}
